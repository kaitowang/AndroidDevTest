<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <Button
        android:id="@+id/oobeNextBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:onClick="onNextBtnClicked"
        android:text="@string/next_btn_label" />

    <Button
        android:id="@+id/oobeBackBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:onClick="onBackBtnClicked"
        android:text="@string/back_btn_label" />

    <FrameLayout
        android:id="@+id/frameLayout1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/oobeBackBtn"
        android:layout_alignParentTop="true" >

        <ViewFlipper
            android:id="@+id/oobePageFlipper"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:longClickable="true" />
    </FrameLayout>

    <ViewFlipper
        android:id="@+id/oobePageIndexSwitcher"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignTop="@id/oobeBackBtn"
        android:layout_toLeftOf="@id/oobeNextBtn"
        android:layout_toRightOf="@id/oobeBackBtn" />

</RelativeLayout>